import React from 'react';
import * as Button from './common/button/Button';

const App = () => (
  <div className="container">
    <div>This is invoice component</div>
    <div>This is total component</div>
    <Button.Add text="HELLO" onClick={() => {}} />
  </div>
);

export default App;
